# Spec：<名称>（<spec-id>，如 plugin-system/v1）

> 级别：Spec（新领域模块 / schema 变更 / AI 协议变更 / daemon 级任务，不可降级为 Plan/Vibe）
> 状态：提案 → 已确认·进行中 → **已完成（YYYY-MM-DD）** / 已废弃
> 提出日期：YYYY-MM-DD · 确认日期： · 完成日期：
> 关联：docs/architecture.md · 相关 spec/plan · dev-log 条目

<!--
使用规则（严格执行，见 docs/specs/README.md）：
1. 文件名 = YYYY-MM-DD-<slug>.md（日期取提案日）；本模板整段复制后从上往下填写。
2. 提案阶段：只填 Why / ADDED / MODIFIED / REMOVED / Unknown，状态=提案，先对齐确认再动手。
3. 确认后：Unknown 的结论写进「决策记录」，状态=已确认·进行中，TDD——测试用例清单先确认。
4. 实施中：每完成一个切片，提交前同步勾选 Tasks 对应项（这是防状态失真的核心纪律）。
5. 完成后：状态改「已完成」并填完成日期；实施偏离如实记入「实施记录」。
-->

## Why

（为什么要做，一段话说清问题与价值；一个月后自己要看得懂。）

## ADDED

### A1 <能力名>

（新增什么：数据结构、契约、行为。协议/schema 变更给出字段级定义。）

## MODIFIED

（改什么：现有行为/文件/协议如何变化，向后兼容性说明。）

## REMOVED

（删什么；没有就写「无」。）

## Unknown（提案阶段必填）

（未决问题、假设、待拍板项。确认后本节结论并入「决策记录」，可删除本节。）

## 决策记录（确认后填写）

- 选项 A vs 选项 B，选 A，因为……
- 否决项及理由（如有）。

## 红线核对

| 红线 | 落实 |
|---|---|
| SQL 只在 storage | |
| 业务只在 core | |
| 前端不碰 DB | |
| 变更留 activity log | |
| 迁移只增不改 | |
| 时间边界唯一入口 local_today_range | |
| 库代码禁 unwrap/expect | |

## 实施记录（完成后填写）

- 切片与对应 commit；与 spec 的偏离及原因。

## 已知限制（如实记录）

## Tasks（切片提交时逐项勾选）

### P1 <阶段名>
- [ ] T-S1 …
- [ ] T-S2 …

## 测试用例清单（TDD：先确认后实现）

**Rust（cargo）**
- [ ] T1 …

**前端（vitest，如涉及）**
- [ ] T7 …

**端到端 / 人工**
- [ ] T12 …
