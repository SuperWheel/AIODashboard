# UI 重设计定稿 · Bento 总控台（2026-08-30）

> 状态：方向已确认，待按切片实施。
> 决策来源：与用户对齐的四项偏好——风格 C（Bento 总控台）、布局（首页网格 + 子页单列）、
> 主题（跟随系统自动双主题）、首页重点（今日任务为主 / 插件卡片舞台 / 快速捕捉 / 统计可视化，全选）。

## 一、设计概念

**「总控台 + 工作室」两层结构**：

- **首页（Today）= 总控台**：Bento 卡片网格，一屏尽览今日状态；插件卡片与核心模块平起平坐。
- **子页（任务/项目/笔记/收件箱/插件）= 工作室**：保持单列居中阅读流，专注处理单一模块。

气质关键词：克制、秩序、信息密度可控。暗色像深夜工作台，亮色像清晨书房——同一套结构，两种氛围。

## 二、设计 Token（Tailwind 4 CSS-first）

全部颜色走语义 token，组件不再写死 `#0f1115` 这类色值：

| Token | 深色 | 浅色 | 用途 |
|---|---|---|---|
| `--color-bg` | `#0B0D12` | `#F7F6F2`（暖纸白） | 窗口底色 |
| `--color-surface` | `#14171F` | `#FFFFFF` | 卡片 |
| `--color-surface-2` | `#1B1F2A` | `#F0EFEA` | 嵌套/悬浮层 |
| `--color-line` | `white/8%` | `black/8%` | 描边 |
| `--color-ink` | `#E8EAF0` | `#1B1D23` | 主文字 |
| `--color-ink-2` | `#8B93A7` | `#6B7180` | 次要文字 |
| `--color-accent` | emerald-400 | emerald-600 | 主强调（完成、主按钮） |
| `--color-danger/warn/info` | rose/amber/sky 400 | 同族 600 | 状态色 |

- 圆角：卡片 `rounded-2xl`(16px)，控件 `rounded-lg`，徽章 `rounded-full`（现状延续）。
- 数字：统计一律 `tabular-nums`，避免跳动。
- 双主题实现：`data-theme` 属性 + `prefers-color-scheme` 媒体查询兜底，
  Tailwind 4 用 `@custom-variant dark` 支持「跟随系统 / 手动固定」两种模式；设置项存 localStorage，后续可落库。

## 三、首页 Bento 网格

容器从 `max-w-3xl` 放宽到 `max-w-6xl`，12 列网格，卡片可跨格：

```text
┌────────────────────────── 12 col ──────────────────────────┐
│ [问候 + 今日进度环 · span 5] [快速捕捉 · span 4] [插件槽 1 · span 3] │
│ [今日任务（含逾期折叠区）· span 8] [统计 2×2 迷你卡 · span 4]        │
│ [最近笔记 · span 4] [活跃项目 · span 4] [插件槽 2 · span 4]          │
└────────────────────────────────────────────────────────────┘
```

- **今日任务为主**：最大卡片，逾期任务置顶红边分组，未完成任务排在插件卡之前。
- **快速捕捉**：独立卡片常驻右上，输入框 + 去向切换（今天到期 / 仅收集到收件箱），
  Enter 即走，不打断当前浏览。这是现有「快速添加」的升级。
- **插件卡片舞台**：`registry.cards` 不再是"插在列表里"，而是网格中的一等格位。
  插件 API 增加可选 `size: "sm" | "md" | "lg"` 声明（默认 `md`=span 4），
  番茄钟声明 `sm`（span 3-4）。**注意：这属于插件协议变更，需同步 PLUGIN_API.md。**
- 窄窗口降级：`<lg` 断点自动堆叠回单列，顺序 = 任务 > 捕捉 > 插件 > 统计 > 其他。

## 四、子页（工作室）

- 保持 `max-w-3xl` 单列居中；统一新组件 `PageHeader`（标题 + 数量徽标 + 主操作按钮）。
- 列表行 hover 浮现操作，完成态 checkbox 打勾微动效。
- 空态：大号 glyph + 一句引导 + 主按钮，不再是只有一行灰字。

## 五、组件与细节升级

- **Sidebar**：毛玻璃（`backdrop-blur`），文字图标 ◎☑▤ 升级为 lucide-react 线性图标，
  底部新增主题切换（跟随系统/深色/浅色三态循环）。
- **ProgressRing**：新组件，SVG 圆环显示今日完成率，放问候卡。
- **StatCard** 重构为迷你卡：数值 + 环比昨日箭头，2×2 收进侧栏格。
- **微动效**：卡片 hover 轻微上浮（translateY(-1px) + 阴影加深）、打勾回弹；
  一律 ≤200ms，并尊重 `prefers-reduced-motion`。
- 可访问性：双主题对比度均 ≥ WCAG AA；所有交互元素有 focus ring。

## 六、实施切片（按 Git 纪律分批）

1. **主题基建**：styles.css token 化 + `@custom-variant` + 主题切换逻辑；全量替换写死色值。
2. **组件库**：ui.tsx 重构 + ProgressRing/PageHeader/QuickCapture 新组件。
3. **Today 页 Bento 化**：网格布局 + 逾期折叠 + 插件槽位 size 协议。
4. **子页打磨**：PageHeader 统一、空态、行交互、Sidebar 图标与毛玻璃。
5. **验收**：`bash scripts/check.sh` 全绿 + 双主题人工走查 + 记 dev-log。

## 七、明确不做（本次范围外）

- 三栏式列表/详情布局（任务详情面板化）——后续单独立项。
- 视图自定义排序/拖拽布局——等插件生态起来后再评估。
- 移动端适配——macOS 桌面优先不变。
