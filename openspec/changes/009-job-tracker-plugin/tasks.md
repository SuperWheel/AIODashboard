# Tasks 009：求职台插件

> 2026-09-08：设计与独立内存 UI 试用版已完成；正式领域提案与以下 TDD 清单待审阅，正式实现项仍未开始。试用交付见[Plan](../../../docs/plans/2026-09-08-job-tracker-ui-trial.md)。

## 本轮设计

- [x] 核对现有 SDK、插件安装手册、样式和 Rust Core 边界。
- [x] 定义字段、默认表格、详情抽屉、阶段与面试语义。
- [x] 制作仅使用演示数据的交互稿。
- [x] 建立 proposal / spec / design / tasks 草案与索引。
- [x] 根据用户反馈补齐整行详情、明确编辑按钮、笔试/面试新增与手动修改、未保存保护。
- [x] 提供「今天求职事项 + 按需关联任务」建议，并制作三个页面间的同源联动演示。
- [x] 按第二次反馈把编辑移入右键菜单，详情/编辑改为全高侧栏；现场核对桌面软件后按实际首页与任务卡片结构修订预览。
- [ ] 产品与技术方案确认；冻结首版范围。

## TDD 清单（先确认，再编写/实现）

- [ ] 公司/岗位必填；同公司不同岗位和批次不互相覆盖；可保留疑似重复。
- [ ] 已投递但时间未知、补录日期精度、阶段前进/跳过/纠错一致；不得伪造投递时间。
- [ ] 日/月/年薪及币种分离；下限/上限、空值、面议、招聘/期望/Offer 区分。
- [ ] 多轮面试下一场聚合；预计/确认/取消/改期/完成；过期待补结果。
- [ ] 本地跨 UTC 日边界、预计日期范围、逾期、归档/终止后提示停止。
- [ ] revision 冲突和 operation_id 幂等；写入失败不产生半条历史；activity actor 与 snapshot 一致。
- [ ] 新增迁移保留旧数据；索引/列表查询；归档和恢复。
- [ ] CLI create → submit → interview → offer → archive/restore 链路，--json 和错误码。
- [ ] 未声明 job.read / job.write 拒绝；停用后旧会话拒绝；SDK mock 与真实路由一致。
- [ ] UI 新建、状态变更、抽屉保存取消、搜索/筛选、空/错/加载/冲突态。
- [ ] 行点击/按钮不误触；任意笔试或面试可改类型、轮次、日期与时刻；完成一轮不自动判通过。
- [ ] 右键/更多/Shift+F10 菜单一致，输入保留原生菜单；边缘避让、关闭和焦点正确；桌面/窄窗口侧栏全高且底栏可操作。
- [ ] Today 卡片的预计/确认、改期/取消、待投递日期筛选和跟进到期；卡片内编辑回流与受限导航。
- [ ] 行动/任务绑定唯一与幂等、日期更新、完成/撤销、关闭后停止提醒；只处理关联任务，CLI 同样有效。
- [ ] 原 TodayTaskCard/TaskCard、进行中/已归档、当日完成留痕、混排/分区、拖拽与热力图不回归；来源标题/日期编辑保持一致。

## Slice 1：Domain / Storage / Core / CLI

- [ ] 实施前重验 schema/协议/宿主版本，冻结数据 DTO、capability 名称和测试清单。
- [ ] 先写失败测试，再新增实体、追加迁移、repository/service；统一 activity、snapshot 与时间规则。
- [ ] 增加对应 CLI 命令和公共 JSON schema 迁移说明。
- [ ] 运行该片最小相关 Rust 门禁，记录证据并同步任务项。

## Slice 2：SDK / Tauri / 插件

- [ ] 扩展双端 manifest 能力允许列表、Core 路由与 SDK/mock；Tauri 仅参数转换。
- [ ] 冻结并实现宿主托管全高侧栏与受限来源导航；处理焦点、主题、卸载/停用清理与卡片 transform 边界。
- [ ] 用现有 TS 脚手架新增求职台插件；注册 view、today_card、command、settings；只申请需要的权限。
- [ ] 实现表格、详情、多轮面试、动态历史、保存/冲突/归档恢复；双主题和卸载清理。
- [ ] 执行相关 SDK/前端检查，修复后再完成下一片。

## Slice 3：正式验收与交付

- [ ] `git diff --check`、`cargo check --workspace`、`bash scripts/check.sh full`；相关范围未再改动则复用通过结果。
- [ ] 独立 DB / 插件目录 / snapshot 的桌面运行：新建、补录、改期、搜索/筛选、关闭/重开、CLI 联动。
- [ ] 浅/深色与 1280/1024/736/360 宽度走查；键盘、抽屉焦点、错误重试与历史正确。
- [ ] 打包 ZIP，实际 GUI 导入、启用、停用、重新启用与数据保留验证；最低宿主版本诊断。
- [ ] 同步 PLUGIN_API / README / architecture / dev-log，proposal 标完成；切片提交只包含本任务改动。

## 后续独立切片

- [ ] 选择采用联动建议后，先冻结行动来源、计划日期、任务关联与 UI 导航契约，再按 TDD 实施。
- [ ] 行动与一次性任务关联（含 Core 今日投影、CLI 完成/撤销、取消与幂等），跨层 full 门禁和隔离数据桌面验收。
- [ ] JSON/CSV 导出与恢复：先明确定义受控文件传输 API，再验证转义、类型、幂等和备份恢复。
- [ ] 可预览、可去重的导入；面试日历；Offer 对比；退出应用后的系统提醒。

## 本轮验证记录

- 交互稿 JavaScript 语法检查通过；片段约 42 KiB，无完整文档标签或外部 API 请求。
- 浏览器实测：8 条初始记录；待投递筛选 2 条；公司搜索 1 条；修改阶段成功；详情显示二面与已完成一面；新增不一致阶段被提示，补成「已投递，时间未记」后成功增加至 9 条且没有伪造时间。
- 首次发现预览 sandbox 不支持原生 form 提交，改为本地按钮校验/更新后，重新验证新增路径成功。
- 检查 1024px 主表/详情、736px 窄窗口、360px 浅/深色回流；360px 根节点宽 328px（预览外壳留边），无根节点横向溢出，730px 表格在自身容器横向滚动。主题能恢复跟随系统。
- 刷新恢复 8 条初始演示记录；没有连接或修改真实求职/宿主数据。
- 正式功能门禁与真实 Tauri 桌面验收未执行，不以设计稿检查替代。

## 2026-09-08 审阅修订验证

- 语法检查通过；整行薪资区域可打开详情，显式编辑按钮可修改城市并保存。
- 将青禾二面改为三面、09-11 16:30，安排历史保留一面，今天卡片显示新轮次与时间；取消远山在线测评后，近期安排计数由 3 变为 2。
- 新增星屿财务笔试：已确认但日期/时刻缺失时拒绝保存；补日期与时刻并显式勾选同步后，岗位阶段变为笔试/测评。
- 下一步加入任务只创建 1 条关联，按钮变为已加入；完成后源行动显示已完成、今日待跟进移除，岗位仍为面试中。
- 松石补录已投递且时间未知后，今天待投递数量由 2 变为 1，计划投递提醒移除，任务数量不增加。
- 资料编辑后关闭弹出未保存提示，选择放弃后不提交修改。
- 浅色 1024px 主表/详情及今天双卡布局、深色 736px 今天页面和 360px 安排表单走查通过；根节点无横向溢出，360px 抽屉宽 328px、高 836px，保存操作可见。
- 预览浏览器错误日志为空；刷新恢复 8 条岗位与 3 条近期确认安排。四件套及开发日志的本地 Markdown 链接检查、`git diff --check` 通过。
- 全部结果为演示数据验证；正式宿主接口、持久化和任务联动仍未实现。

## 2026-09-08 第二次审阅修订验证

- 实际打开本机 AIODashboard 的今天/任务页，并与当前仓库组件核对首页 12 列、右栏、后置插件槽位、任务双列/热力图/归档结构；只浏览，未操作真实任务打卡或编辑。
- 列表独立编辑按钮数量为 0；在行的薪资区域右键打开菜单，不会直接弹详情。菜单「编辑投递信息」可以修改城市并保存；详情更多菜单与逐条安排菜单可以修改轮次和时间。
- Shift+F10 打开菜单、Escape 关闭通过；360px 右键菜单四边均在应用区域内。表单未保存关闭提示仍有效，放弃不写入。
- 1024px 预览中详情与应用区域同高 820px，上下空隙均为 0；从已经滚动 456px 的今天页面打开仍为 0。360px 面板为 328×820，操作底栏在面板内且根节点没有横向溢出。
- 关联行动后原生样式任务由 3 张示例卡变为 4 张；完成后进行中回到 3 张、已归档有 1 张，今天保留完成记录。撤销恢复到进行中，岗位保持面试中；类型分区展示日/周/月三组。
- 1024px 浅色首页/任务/详情，736px 浅深色首页、360px 浅深色编辑器走查通过；修正窄侧栏搜索文字挤压。预览 JavaScript 语法检查通过，浏览器无错误日志。
- 仍为可审阅的内存示意，不代表正式 TodayTaskCard/TaskCard 已接通求职来源，也不覆盖真实拖拽/持久化/CLI/安装验收；正式功能门禁未执行。


## 2026-09-08 独立界面试用交付

- 用户明确选择演示数据试用；按独立 Plan 完成 `com.leeyl.job-tracker-trial`，没有勾选本清单中的正式领域实现项。
- GUI 新安装/启用、右键资料编辑、轮次/日期时刻修改与 Today 同步、滚动卡片打开全高详情、深浅主题、任务预览完成/撤销、更新重载及停用重新启用通过。
- 最终 frontend、插件测试与语法检查通过；为启用新版宿主追加 Rust 完整检查通过。原真实任务与打卡账本逐条核对不变，详情见[试用 Plan](../../../docs/plans/2026-09-08-job-tracker-ui-trial.md)。
- 正式 Core/CLI 持久化、来源关联和宿主托管覆盖层仍未实现；试用记录重载后恢复，不能用于保存真实求职记录。
